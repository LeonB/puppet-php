# MANAGED BY PUPPET!
# modules/php/templates/pool.conf.erb
# Changes WILL BE overwritten! Any permanent changes must be checked
# into git.

; Start a new pool named '<%= name %>'
[<%= name %>]
listen  = 127.0.0.1:<%= port %>
user    = <%= user %>
group   = <%= group %>
# chroot  = /home/<%= name %>/

pm = dynamic
pm.start_servers = 1
pm.min_spare_servers = 1
pm.max_spare_servers = 2
pm.max_children = 2
